/**
 * Definition for singly-linked list.
 * */
class ListNode {
  val: number;
  next: ListNode | null;
  constructor(val?: number, next?: ListNode | null) {
    this.val = val === undefined ? 0 : val;
    this.next = next === undefined ? null : next;
  }
}

const convertListToNode = (list:number[]):ListNode=>{

  return null
}
// 链表不能反向，但数组可以reverse（这题不做）
export function to6_reversePrint(head: ListNode | null): number[] {
  return null
}
